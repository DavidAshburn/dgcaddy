<!DOCTYPE html>
<html>
  <head>
    <title>Dgcaddy</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://fonts.googleapis.com/css2?&family=Monoton&family=Raleway:wght@300;400;600;700&family=Righteous&display=swap" rel="stylesheet">    
  </head>
  
  <% if notice %>
    <div class="flex flex-row absolute left-10 bottom-10">
      <%= render 'layouts/alerts' %>
    </div>
  <% end %>
    
  <% if user_signed_in? %>
    <div class="ml-5 mt-5">
      <%= render 'layouts/nav' %>
    </div>
  <% end %>



  <body class="bg-fixed bg-oversize bg-gradient-to-bl from-rose-200 via-blue-400 to-indigo-600">
    <main class="container mt-5 p-5 flex flex-col items-center w-[100vw]">
      <%= yield %>
    </main>
  </body>

</html>